apiVersion: v1
kind: Service
metadata:
  name: {{ include "gizmosql-mcp.fullname" . }}
  labels:
    {{- include "gizmosql-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gizmosql-mcp.selectorLabels" . | nindent 4 }}
